Currently supported features:
- basic CFG:
    For, While Statements
- Assignments like a=1, a=b, a = a+b, 
Not supported Syntax:
- return Variables (Use)
- break (interrupts CFG)
- For (int i = 0; i ; i +=1)
- a += 1; a -= b; a /= 2, a *= b;